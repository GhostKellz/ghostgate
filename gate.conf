# Main configuration file for GhostGate
# Example configuration

server:
  port: 8080
  static_dir: ./static
  backend: ""

logging:
  level: "info"
  format: "text"

proxy:
  routes:
    - path: "/api"
      backend: "http://localhost:3000"
